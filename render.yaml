# NOTE: Render removed free PostgreSQL in 2023.
# Use Railway instead (see README) — it includes free PostgreSQL.
# If you have a paid Render plan or an external DATABASE_URL, you can use this:
#
# services:
#   - type: web
#     name: unplug
#     runtime: docker
#     envVars:
#       - key: DATABASE_URL
#         value: <your-postgresql-url>
